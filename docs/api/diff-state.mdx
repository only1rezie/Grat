---
title: "diff_state"
description: "Compares pre and post execution ledger states."
---

```rust
pub fn diff_state(
    before: &LedgerState,
    after: &LedgerState
) -> Result<StateDiff, PrismError>
```

Produces a structured comparison of ledger entries, highlighting created, updated, and deleted keys.
